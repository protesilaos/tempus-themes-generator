# vi: ft=sh

# ============================  
# scheme: Future
# scheme family: Tempus
# description: Dark theme with colours inspired by concept art of outer space 
# author: Protesilaos Stavrou (https://protesilaos.com/)
# ============================  

# This dark theme only uses the col0 and col8 values for backgrounds.
# All other colours are foreground values.

# All fgs have a contrast ratio of >= 7.00:1 (WCAG AAA) against the bgs.
# Exception is col8 on col0 or vice versa (these are mutually exclusive)

# It is possible to use foregrounds as backgrounds, by reversing the above.
# DO NOT blend foregrounds together. The contrast ratio is sub-standard.

# Meta data
# ----------------------------
tempus_theme_name='Future'
tempus_theme_name_full='Tempus Future'
tempus_theme_name_slug='future'
tempus_theme_name_slug_full='tempus-future'
tempus_theme_name_slug_full_underscore='tempus_future'
tempus_theme_description='Dark theme with colours inspired by concept art of outer space (WCAG AAA compliant)'
tempus_theme_wcag='WCAG AAA'
tempus_theme_variant='dark'
tempus_theme_author='Protesilaos Stavrou (https://protesilaos.com)'

# Colour values (HEX)
# ----------------------------
col0=090a18       # black variant
col1=ff778a       # red variant
col2=6ab539       # green variant
col3=bfa01a       # yellow variant
col4=4aaed3       # blue variant
col5=e58a82       # magenta variant
col6=29b3bb       # cyan variant
col7=a59ebd       # white variant
col8=260e22       # bright black variant
col9=f78e2f       # bright red variant
col10=60ba80      # bright green variant
col11=de9b1d      # bright yellow variant
col12=8ba7ea      # bright blue variant
col13=e08bd6      # bright magenta variant
col14=2cbab6      # bright cyan variant
col15=b4abac      # bright white variant
